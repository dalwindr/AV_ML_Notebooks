   git --version
   git -global user.name "dalwindr"
   git config --global user.name "dalwindr"
   git config --global user.email "dalwindr@gmail.com"
   git status
   git add *.scala
   git commit -m "basic scala POC"
   git commit -m "scala with spark stream example and hbase example"
   git reset 
   git remote add origin https://github.com/dalwindr/scala_spark_POC.git
   git push -u origin master
